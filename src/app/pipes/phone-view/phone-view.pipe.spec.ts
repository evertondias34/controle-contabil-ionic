import { PhoneViewPipe } from "./phone-view.pipe";

describe("PhoneViewPipe", () => {
  it("create an instance", () => {
    const pipe = new PhoneViewPipe();
    expect(pipe).toBeTruthy();
  });
});
